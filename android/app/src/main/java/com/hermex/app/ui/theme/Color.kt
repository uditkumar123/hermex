package com.hermex.app.ui.theme

import androidx.compose.ui.graphics.Color

// Primary
val HermexBlue = Color(0xFF007AFF)
val HermexBlueDark = Color(0xFF0A84FF)

// Backgrounds
val BackgroundLight = Color(0xFFF2F2F7)
val BackgroundDark = Color(0xFF000000)
val SurfaceLight = Color(0xFFFFFFFF)
val SurfaceDark = Color(0xFF1C1C1E)
val SurfaceVariantLight = Color(0xFFF2F2F7)
val SurfaceVariantDark = Color(0xFF2C2C2E)

// Text
val TextPrimaryLight = Color(0xFF000000)
val TextPrimaryDark = Color(0xFFFFFFFF)
val TextSecondaryLight = Color(0xFF3C3C43).copy(alpha = 0.6f)
val TextSecondaryDark = Color(0xFFEBEBF5).copy(alpha = 0.6f)

// Messages
val UserBubbleLight = Color(0xFF007AFF)
val UserBubbleDark = Color(0xFF0A84FF)
val AssistantBubbleLight = Color(0xFFE9E9EB)
val AssistantBubbleDark = Color(0xFF2C2C2E)

// Status
val SuccessGreen = Color(0xFF34C759)
val ErrorRed = Color(0xFFFF3B30)
val WarningOrange = Color(0xFFFF9500)

// Tool calls
val ToolCallBorder = Color(0xFF007AFF).copy(alpha = 0.3f)
val ReasoningBorder = Color(0xFF8E8E93)

// Divider
val DividerLight = Color(0xFF3C3C43).copy(alpha = 0.12f)
val DividerDark = Color(0xFF545458).copy(alpha = 0.35f)
