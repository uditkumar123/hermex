# Hermex Android — Project State

**Milestone:** MVP (Chat + Sessions)
**Current Phase:** MILESTONE COMPLETE
**Overall Progress:** 5/5 phases complete

---

## Phase Status

| Phase | Status | Progress |
|-------|--------|----------|
| 1. Foundation | ✅ COMPLETE | 32 files, build compiles |
| 2. Auth + Onboarding | ✅ COMPLETE | Auth flow, connection, settings |
| 3. Session List | ✅ COMPLETE | Swipe actions, search, create dialog, profile switching |
| 4. Chat + Streaming | ✅ COMPLETE | Streaming, reasoning blocks, approval/clarification overlays |
| 5. Polish + Store | ✅ COMPLETE | Offline cache, splash screen, store prep |

---

## Build Status
- **Last build:** 2026-07-02 ✅ SUCCESSFUL
- **APK:** `android/app/build/outputs/apk/debug/app-debug.apk`
- **Warnings:** 1 (deprecated statusBarColor API)

## Recent Changes (2026-07-02)

### AuthStatusResponse Model Fix
- Updated to match official API docs (`authenticated` + `password_required` fields)
- Added backward compatibility for older server versions
- `isAuthRequired` property now checks both new and legacy fields

### API Endpoints Added
- Session export/import (`/api/session/export`, `/api/session/import`)
- YOLO mode (`/api/session/yolo`)
- Goal & BTW (`/api/goal`, `/api/btw`)
- Background tasks (`/api/background/*`)
- TTS (`/api/tts`)
- Profile active (`/api/profile/active`)
- Metrics tracking (`/api/metrics/track`)
- Last commit (`/api/last-commit`)
- Agent restart (`/api/agent/restart`)

### SSE Event Types Added
- `apperror`, `metering`, `compressed`, `compressing`
- `warning`, `context_status`, `todo_state`
- `goal`, `goal_continue`

### SessionSummary Model Updates
- Added `displayTitle`, `cacheHitPercent`, `windowUsagePercent`
- Added `worktreePath`, `parentSessionId`, `relationshipType`
- Added `readOnly`, `attention`
- Renamed `displayTitle` property to `effectiveDisplayTitle` to avoid conflicts

### SessionListViewModel
- Added `searchSessionsRemote` method with debounced search

---

## File Inventory

### Data Layer (14 files)
- `data/model/SessionModels.kt` — SessionSummary, SessionDetail, SessionResponse, etc.
- `data/model/ChatModels.kt` — ChatMessage, ChatStartResponse, ModelsResponse, etc.
- `data/model/SSEModels.kt` — SSEEvent, ToolStreamEvent, ApprovalPendingResponse, etc.
- `data/model/AuthModels.kt` — HealthResponse, AuthStatusResponse, LoginResponse
- `data/model/CommonModels.kt` — APIError, CustomHeader
- `data/api/ApiClient.kt` — HermesApi Retrofit interface + request body classes
- `data/api/RetrofitProvider.kt` — Retrofit/OkHttp setup, cookie jar
- `data/api/SSEClient.kt` — OkHttp SSE streaming
- `data/api/SseEventDecoder.kt` — SSE event parsing
- `data/auth/AuthManager.kt` — Auth state machine
- `data/auth/ServerRegistry.kt` — Multi-server storage
- `data/auth/CustomHeaders.kt` — Custom HTTP headers
- `data/repository/SessionRepository.kt` — Session CRUD
- `data/repository/ChatRepository.kt` — Chat start/stream/cancel

### UI Layer (14 files)
- `ui/theme/Theme.kt`, `Color.kt`, `Type.kt`
- `ui/navigation/NavGraph.kt`
- `ui/auth/OnboardingScreen.kt`, `ConnectScreen.kt`, `AuthViewModel.kt`
- `ui/sessionlist/SessionListScreen.kt`, `SessionListViewModel.kt`
- `ui/chat/ChatScreen.kt`, `ChatViewModel.kt`, `MessageBubbleView.kt`, `ChatComposerView.kt`, `ToolCallCardView.kt`

### App Core (4 files)
- `HermexApp.kt`, `MainActivity.kt`
- `util/Constants.kt`, `util/Extensions.kt`

---

## Blockers
None currently.

## Decisions
- Package name: `com.hermex.app`
- Min SDK: 26 (Android 8.0)
- Target SDK: 35
- Kotlin 2.1.0, Compose BOM 2024.12.01
- No DI framework (manual singletons)
- Gradle 8.9 (cached on machine)
